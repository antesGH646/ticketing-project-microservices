spring:
  application:
    name: user-service
  profiles:
    active: user_service
  cloud:
    config:
      name: configuration-client
      uri: http://configserver:8888
      fail-fast: true
      retry:
        max-attempts: 200
        max-interval: 10000